1. 병합 쉘 영역 구분을 Floodfill 알고리즘을 이용해서 진행하기. 각 병합 쉘마다 각기 다른 group index 설정하고 새로 merge_matrix를 만들어서 개인 쉘은 0, 병합 쉘은 group index로 구분지을 수 있게 함. x span과 y span도 유지.
2. 데이터 추출이 끝나고 저장할때 x y 등 쓸모없는 값들 제거하고 오직 데이터값만 matrix에 정확한 위치에 남도록 하고 merge_matrix만 유지시킴.
3. parsing 패턴 만들어야함. 테이블을 구분하고 각 구분에 맞춰 테이블을 파싱해야함.



----
1. Table 클래스 생성.
	Table 클래스는 다음 Attribute들을 지님.
	- x coord list
	- y coord list
	- cell matrix
	- merge matrix
	
	cell matrix는 nxm 크기의 2차원 리스트이며 n = len(x) - 1, m - len(y) - 1의 길이를 지닌다. 각 원소는 딕셔너리 형태이며 다음 정보를 지닌다.
	- row		-> 행 번호
	- col		-> 열 번호
	- data		-> 내부 값
	단 만약 병합 쉘의 경우엔 시작 지점만 딕셔너리 형태를 지니고 나머지 cell은 None로 처리한다.

	merge matrix는 cell matrix와 같은 크기를 지닌 2차원 리스트이며 각 원소는 해당 위치의 쉘이 어떤 병합 그룹에 속하는지 알린다. 모든 기본 1x1 쉘은 그룹 0이며 새 병합 그룹마다 index를 1씩 증가시키며 저장한다.
