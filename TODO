거의 다 끝나간다.
테이블 영역 분할도 끝났고 cell 영역도 확실하게 처리 가능하다.
다음으로 남은 과제는 다음과 같다:
1. 병합된 셀의 영역 내부에 포함되지 않는 부분을 잘라내는 방법.

*****2. 가로 세로 문자열의 비교를 더 정확하게 하기. 혹은 문자열이 가로인지 세로인지 확실히 알 수 있게 하기. *****

3. 셀에 데이터가 비어있을 경우 셀 자체를 None으로 처리하기.
4. 테이블 클래스 생성하여 셀 영역 구분할 때 병합 셀이 어떤 셀들을 포함하는지 정보 처리해야함. 첫 번째 셀에 group이 아니라 자기 영역을 표현하는 필드 추가 (예: (r1, c1), (r2, c2), (r3, c3)... 혹은 (x, y, xspan, yspan), (x, y, xsapn, yspan)...). 

최종적으로 json 파일 반환함으로 서버측 코드 완료.
